#include <iostream>
#include <string.h>
using namespace std;

int main(){
    char kalimat[80];
    char besar, kecil;
    char* hasil;
    cout << "Masukan Kalimat: ";
    cin.getline(kalimat, 80);

    hasil = strchr(kalimat, besar, kecil);
    while(hasil != NULL){
        cout << 
    }

    strchr(kalimat);
    cout << kalimat << endl;
    return 0;
}